limit_req_zone $binary_remote_addr zone=scanner_api:10m rate=10r/s;

server {
    listen 80;
    server_name scanner.aksenovaks.online;

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name scanner.aksenovaks.online;

    ssl_certificate /etc/letsencrypt/live/scanner.aksenovaks.online/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/scanner.aksenovaks.online/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    location = / {
        return 302 /scanner;
    }

    location /scanner {
        client_max_body_size 64k;
        proxy_pass http://bot:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    location /scanner/api/ {
        limit_req zone=scanner_api burst=30 nodelay;
        client_max_body_size 8k;
        proxy_pass http://bot:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    location / {
        return 404;
    }
}

server {
    listen 8081;
    server_name _;

    location = / {
        return 302 /scanner;
    }

    location /scanner {
        client_max_body_size 64k;
        proxy_pass http://bot:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /scanner/api/ {
        limit_req zone=scanner_api burst=30 nodelay;
        client_max_body_size 8k;
        proxy_pass http://bot:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        return 404;
    }
}

server {
    listen 8443 ssl;
    server_name _;

    ssl_certificate /etc/nginx/certs/scanner.crt;
    ssl_certificate_key /etc/nginx/certs/scanner.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    location = / {
        return 302 /scanner;
    }

    location /scanner {
        client_max_body_size 64k;
        proxy_pass http://bot:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    location /scanner/api/ {
        limit_req zone=scanner_api burst=30 nodelay;
        client_max_body_size 8k;
        proxy_pass http://bot:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }

    location / {
        return 404;
    }
}
